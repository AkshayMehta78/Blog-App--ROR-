<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>
 
<p>
  <strong>Text:</strong>
  <%= @article.text %>
</p>
 
 <p>
  <strong>Attachment:</strong>
  <%= image_tag @article.attachment.url(:thumb)  %> 
</p>

<p>
  <strong>Tags:</strong>
<% @article.tags.any? %>
  <% @article.tags.each do |tag| %>
  <%= link_to tag.name, articles_url(:tag => tag.name) %>
<% end %>
</p>
<br>

<p><%= link_to "I have Read this artcile" , 'https://www.facebook.com/dialog/feed?
  app_id=473701529432821
  &display=popup&caption=An%20example%20caption 
  &link=http://genii.in
  &redirect_uri=http://localhost:3000' %></p>

<h2>Comments</h2>
<% @article.comments.each do |comment| %>
  <p>
    <strong>Commenter:</strong>
    <%= comment.commenter %>
  </p>
 
  <p>
    <strong>Comment:</strong>
    <%= comment.body %>
  </p>
<% end %>

<h2>Add a comment:</h2>
<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.label :commenter %><br>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
 
<%= link_to 'Edit Article', edit_article_path(@article) %> |
<%= link_to 'Back to Articles', articles_path %>